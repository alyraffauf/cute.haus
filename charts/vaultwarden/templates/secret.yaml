apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.envFromSecret }}
  labels:
    app: vaultwarden
type: Opaque
stringData:
  {{- range $k, $v := .Values.secret }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
