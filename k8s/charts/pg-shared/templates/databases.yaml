{{- range $name, $cfg := .Values.roles }}
{{- $db := $cfg.database | default $name }}
---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: {{ $db }}
spec:
  name: {{ $db }}
  owner: {{ $name }}
  cluster:
    name: {{ $.Values.cluster.name }}
{{- end }}
