replicaCount: 1
strategy: Recreate
terminationGracePeriodSeconds: 30

image:
  repository: lscr.io/linuxserver/ombi
  tag: latest@sha256:db3642d3fd492c399e1c879fe38fe77b59db6ca2e966b0d3df2aad7d376489da
  pullPolicy: IfNotPresent

podSecurityContext:
  fsGroup: 1000

resources:
  requests:
    cpu: 50m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi

ports:
  - name: http
    containerPort: 3579

service:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: 3579

env:
  PUID: "1000"
  PGID: "1000"
  TZ: America/New_York

persistence:
  enabled: true
  storageClassName: longhorn
  size: 2Gi
  mountPath: /config

failover:
  fastTolerationSeconds: 60

probes:
  startup:
    httpGet: { path: /, port: http }
    periodSeconds: 10
    failureThreshold: 60
  liveness:
    httpGet: { path: /, port: http }
    periodSeconds: 30
  readiness:
    httpGet: { path: /, port: http }
    periodSeconds: 10
    failureThreshold: 3

ingress:
  enabled: true
  className: traefik
  routes:
    - host: ombi.cute.haus
      tlsSecret: cute-haus-tls
